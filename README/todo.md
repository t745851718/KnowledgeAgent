## 主要待办和风险

  - 入库任务使用进程内 asyncio.Task，进程重启后无法恢复。
  - 请求幂等锁和文档写入锁只在单进程内有效，多 worker 会失效。
  - 列表接口先读取大量记录再用 Python 分页，不适合大数据量。
  - 没有正式登录和权限系统。
  - 没有部署配置、容器编排或 CI。
  - 图文融合入库已实现（MinerU 图片 + 对应文本生成 dense，描述文本生成 sparse）；已有文档需重新入库，并另行验证真实云端图文流程。
  - 后续需结合数据库幂等与持久化任务队列设计 checkpoint 恢复，不能直接开启有副作用节点的重试。
